[Unit]

Description=Template of daemon service
After=network.target

[Service]

ExecStart=/usr/bin/python3 ${path}/publisher.py $receiver $date
Environment=PYTHONUNBUFFERED=1
Restart=on-failure
Type=notify
User=$user

[Install]

WantedBy=default.target
