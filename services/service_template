[Unit]

Description=Template of daemon service
After=network.target

[Service]

User=$user
Group=$user
#VIRTUAL_ENV=/home/admin/venv
#Environment=PATH=$VIRTUAL_ENV/bin/python
#Environment="VIRTUAL_ENV=/home/admin/venv"
#Environment="PATH=$VIRTUAL_ENV/bin/python"
Restart=on-failure
Type=notify

ExecStart=source /home/admin/venv/bin/activate && /home/admin/venv/bin/python ${path}/src/publisher.py $receiver

[Install]

WantedBy=default.target
